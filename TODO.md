# TODO

- [ ] Inspect `src/app/about/page.tsx` UI structure requirements
- [x] Update `src/app/facilities/page.tsx` to visually match `about/page.tsx` UI (hero + editorial blocks + lower quote) WITHOUT changing functionality/CTA behavior and without fully replacing unrelated logic

- [x] Ensure facilities data mapping works with existing `facilitiesPage.json` structure

- [x] Build/lint checks (if available): `npm run lint` and `npm run build`


